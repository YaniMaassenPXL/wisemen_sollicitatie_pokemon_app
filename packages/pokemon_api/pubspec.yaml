name: pokemon_api
description: The interface and models for an API providing access to pokemon.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  equatable: ^2.0.3
  json_annotation: ^4.6.0
  http: ^0.13.5

dev_dependencies:
  build_runner: ^2.2.0
  json_serializable: ^6.3.1
